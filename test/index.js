const { assert } = require('chai');

describe('document fingerprint test suite', () => {
    describe('reading data from a pdf', () => {
        it('should read data in hex', () => {
            assert.equal(1, 1);
        });
    });
});